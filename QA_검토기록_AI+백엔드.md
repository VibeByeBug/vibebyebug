# QA 검토기록 — AI + 백엔드 연동

작성 : 2026-09-05 · 

---

## ① 질문 요약으로 인한 지연

### 화면에 떠야 하는 게 4가지

질문이 끝나면 발표자 화면에 이런 게 뜸.

```
┌─────────────────────────────────────┐
│ 질문 요약:  정확도를 어떻게 쟀나?   │  ← ①
│ 유형:  [근거]                       │  ← ②
│ 키워드:  질문 45개 · R@3 · 하이브리드  │  ← ③
│ 출처:  11번 슬라이드                │  ← ④
└─────────────────────────────────────┘
```

각각 걸리는 시간이 다름.

| | 무엇 | 만드는 법 | 걸리는 시간 |
|---|---|---|---|
| ② | 유형 태그 | 정규식 | 0ms |
| ③ | 키워드 | 슬라이드에서 뽑기 | 1.2ms |
| ④ | 출처 | 검색 | 169ms |
| ① | **질문 요약** | **LLM(Gemini)에 물어보기** | **300~800ms** |

**①번 한 줄이 나머지 셋을 다 합친 것보다 오래 걸림.**

### 그냥 합치면 예산 초과

```
말 끝남 ─845ms─→ Gemini 요약 ─600ms─→ 검색 ─169ms─→ 화면
                 └──────── 합계 1,614ms ────────┘      예산 1,000ms
```

요약 하나 때문에 예산이 무너짐.

### 선택지 3개

**(a) 요약을 안 만든다** — 인식된 질문 원문을 그대로 띄움

- 지연 추가 : +0ms
- 근거 : 발표자는 방금 그 질문을 귀로 들었음. 화면에 필요한 건 요약이 아니라 근거일 수 있음
- 반론 : 계획서 출발점이 "장황한 질문 → 인지 과부하"였음. 한 줄 요약이 차별성의 일부일 수 있음

**(b) 지금처럼 요약을 만든다**

- 지연 추가 : +300~800ms (실측 안 됨)
- 대신 검색 프리셋을 `fast`로 낮춰야 할 수 있음 → **정확도 88.9% → 80%를 요약과 맞바꾸는 셈**

**(c) 요약과 검색을 동시에 던진다** (`asyncio.gather`)

```
말 끝남 ┬→ 검색 169ms  → 유형·키워드·출처 먼저 표시
        └→ 요약 600ms  → 요약 줄이 나중에 채워짐
```


### 요약 소요 시간 측정 방법 — 3줄

`main.py` 맨 위에 `import time` 추가하고, `websocket_endpoint` 안 `try` 블록을 이렇게.

```python
            try:
                t0 = time.perf_counter()                                    # 추가
                response = await model.generate_content_async(user_voice_text)
                summary_ms = round((time.perf_counter() - t0) * 1000)       # 추가
                print(f"Gemini 요약 지연: {summary_ms}ms")                   # 추가
                ai_summary = response.text.strip()
            except Exception as e:
                ai_summary = f"[API 오류] {str(e)}"
```

서버 띄운 터미널에 `Gemini 요약 지연: 640ms` 형태로 찍힘. 10회 정도 반복해 범위 파악.

### 판정 기준

| 실측값 | 결론 |
|---|---|
| 200ms 이하 | (b) 그대로 둬도 무방 |
| 300~600ms | (c) 병렬 처리 필요 |
| 800ms 이상 | (a) 실전에서 빼고 사후 리포트로 이동 |

### 측정 기록

| 회차 | 요약 지연(ms) | 질문 길이 | 비고 |
|---|---|---|---|
| 1 | | | |
| 2 | | | |
| 3 | | | |
| … | | | |
| **평균** | | | |
| **판정** | | | (a)/(b)/(c) 중 |

---

## ② requirements.txt 

### 무슨 문제인가

GitHub에 올라가는 건 **코드 파일뿐**이고, 각자 설치한 라이브러리는 안 따라감.

```
GitHub에 올라감    :  main.py, index.html, pipeline.py ...
GitHub에 안 올라감 :  각자 pip install 한 것들 (fastapi, numpy, ...)
```

그래서 남이 코드를 받아도 바로 못 돌림. 코드를 읽어가며 뭐가 필요한지 하나씩 찾아 설치해야 함.
(실제로 백엔드 서버를 처음 띄울 때 5개를 직접 찾아 넣어야 했음)

`requirements.txt` 는 그 목록을 적어 코드와 함께 올리는 파일. 받는 쪽은 한 줄로 끝남.

```
pip install -r requirements.txt
```

### 버전까지 맞추려면 `==` 로 고정해야 함

| 적는 방식 | 결과 |
|---|---|
| `fastapi` | 설치 시점의 최신 버전이 깔림 → 사람마다 다를 수 있음 |
| `fastapi==0.141.1` | 누가 언제 깔아도 같은 버전 |

`pip install -r` 자체가 버전을 맞춰주는 게 아니라, **파일에 버전이 적혀 있어야** 맞춰짐.


### AI 쪽에 필요한 것 (코드 import 기준, 버전 미확인)

```
numpy
sentence-transformers
kiwipiepy
python-pptx          # import pptx
PyMuPDF              # import fitz
google-genai
langgraph
anthropic            # caption.py 제공사 선택지
openai               # caption.py 제공사 선택지
pywin32              # render.py, PowerPoint COM (윈도우 전용)
```

> `pywin32`는 윈도우에서만 설치됨. 맥 사용자가 있으면 `render.py` 실행이 막히므로 확인 필요.
> 버전은 이예진 님이 자기 환경에서 뽑아야 정확함.

### 해야하는 이유

9/12 관문 1에서 통합 검증을 하려면 서로 코드를 돌려봐야 함.
파일이 없으면 팀원 3명이 각자 같은 삽질을 반복하고, 그 자리에서 시간을 잡아먹음.
"제 컴퓨터에선 되는데요"가 나오기 딱 좋은 상황.

