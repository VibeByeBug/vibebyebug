import type { RecognizedQuestionData } from '../types/flow';

export const mockRecognizedQuestion: RecognizedQuestionData = {
  partialText: '재무 지표에서 매출...',
  finalText: '재무 지표에서 매출 성장률의 근거는 무엇인가요?',
  isConfirmed: true,
};
